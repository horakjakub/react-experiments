import useThrottleDebounce from '..';
import { renderHook, act } from '@testing-library/react-hooks';

describe('useDebounce()', () => {
  it('should', () => {

    expect(null).toBeNull();
  });
});

